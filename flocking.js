

function main(){
    var canvas = document.querySelector("canvas");
    var gl = canvas.getContext("webgl");
    
    if(gl === null){
        alert("could not load webgl");
    }
    
    // make trianlgle object to hold info
    // make array of triangle vertexes
    // make shaders
    // animate
}
